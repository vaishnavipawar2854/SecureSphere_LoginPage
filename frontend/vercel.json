{
  "version": 2,
  "installCommand": "npm install --prefix",
  "buildCommand": "npm run build --prefix",
  "outputDirectory": "/dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
